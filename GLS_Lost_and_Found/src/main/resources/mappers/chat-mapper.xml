<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Chat">
	<insert id="insertChat">
		insert into chat (seq, fromID, toID, chatContent)
		values
		(#{seq}, #{fromID}, #{toID}, #{chatContent})
	</insert>
	<update id="updateChat">
		update chat
		set 
		fromID=#{fromID}, 
		toID=#{toID},
		chatContent=#{chatContent}
		where seq=#{seq}
	</update>
	<delete id="deleteChat">
		delete from chat where seq=#{seq}
	</delete>
	<select id="getChat" resultType="chat">
		select * from chat where
		seq=#{seq}
	</select>
	<select id="getChatList" resultType="chat">
		select * from chat order
		by seq desc
	</select>

</mapper>